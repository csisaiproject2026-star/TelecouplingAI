services:

  # ── Nginx entry point (port 80) ─────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: tele-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api-server
      - frontend-ui
    restart: always

  # ── FastAPI backend ──────────────────────────────────────────────────────
  api-server:
    build: ./backend
    image: csic_backend:latest
    container_name: tele-backend
    env_file: .env.docker
    expose:
      - "8000"
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
      - ./Systematic_tests:/home/csisaiproject2026/csis-platform/telecouplingAI-project/Systematic_tests
      - ./.claude:/.claude
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: always

  # ── Celery workers — one per tool queue (P1: concurrency=1 per tool) ────
  # Each InVEST tool gets its own worker process so at most ONE instance of
  # each tool runs at any time, eliminating GDAL multi-process conflicts.
  # Users requesting the same tool while it is busy will queue in Redis.
  # Total max memory: ~14 GB — safe for a 32 GB server.

  celery-worker-net:
    image: csic_backend:latest
    container_name: tele-celery-net
    command: >
      celery -A workers.task_queue worker
      --queues=q_net
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-net@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-cbc-pre:
    image: csic_backend:latest
    container_name: tele-celery-cbc-pre
    command: >
      celery -A workers.task_queue worker
      --queues=q_cbc_pre
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-cbc-pre@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-cbc-main:
    image: csic_backend:latest
    container_name: tele-celery-cbc-main
    command: >
      celery -A workers.task_queue worker
      --queues=q_cbc_main
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-cbc-main@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-swy:
    image: csic_backend:latest
    container_name: tele-celery-swy
    command: >
      celery -A workers.task_queue worker
      --queues=q_swy
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-swy@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 4G

  celery-worker-crop-pct:
    image: csic_backend:latest
    container_name: tele-celery-crop-pct
    command: >
      celery -A workers.task_queue worker
      --queues=q_crop_pct
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-crop-pct@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-crop-reg:
    image: csic_backend:latest
    container_name: tele-celery-crop-reg
    command: >
      celery -A workers.task_queue worker
      --queues=q_crop_reg
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-crop-reg@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-render:
    image: csic_backend:latest
    container_name: tele-celery-render
    command: >
      celery -A workers.task_queue worker
      --queues=q_render,q_default
      --concurrency=2
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-render@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  # ── New InVEST tool workers ───────────────────────────────────────────────

  celery-worker-carbon:
    image: csic_backend:latest
    container_name: tele-celery-carbon
    command: >
      celery -A workers.task_queue worker
      --queues=q_carbon
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-carbon@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-habitat-quality:
    image: csic_backend:latest
    container_name: tele-celery-habitat-quality
    command: >
      celery -A workers.task_queue worker
      --queues=q_habitat_quality
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-habitat-quality@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-awy:
    image: csic_backend:latest
    container_name: tele-celery-awy
    command: >
      celery -A workers.task_queue worker
      --queues=q_awy
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-awy@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-forest-carbon:
    image: csic_backend:latest
    container_name: tele-celery-forest-carbon
    command: >
      celery -A workers.task_queue worker
      --queues=q_forest_carbon
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-forest-carbon@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-pollination:
    image: csic_backend:latest
    container_name: tele-celery-pollination
    command: >
      celery -A workers.task_queue worker
      --queues=q_pollination
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-pollination@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-delineateit:
    image: csic_backend:latest
    container_name: tele-celery-delineateit
    command: >
      celery -A workers.task_queue worker
      --queues=q_delineateit
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-delineateit@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-routedem:
    image: csic_backend:latest
    container_name: tele-celery-routedem
    command: >
      celery -A workers.task_queue worker
      --queues=q_routedem
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-routedem@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-sdr:
    image: csic_backend:latest
    container_name: tele-celery-sdr
    command: >
      celery -A workers.task_queue worker
      --queues=q_sdr
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-sdr@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-ndr:
    image: csic_backend:latest
    container_name: tele-celery-ndr
    command: >
      celery -A workers.task_queue worker
      --queues=q_ndr
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-ndr@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-urban-cooling:
    image: csic_backend:latest
    container_name: tele-celery-urban-cooling
    command: >
      celery -A workers.task_queue worker
      --queues=q_urban_cooling
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-urban-cooling@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-urban-flood:
    image: csic_backend:latest
    container_name: tele-celery-urban-flood
    command: >
      celery -A workers.task_queue worker
      --queues=q_urban_flood
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-urban-flood@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-urban-stormwater:
    image: csic_backend:latest
    container_name: tele-celery-urban-stormwater
    command: >
      celery -A workers.task_queue worker
      --queues=q_urban_stormwater
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-urban-stormwater@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-urban-nature:
    image: csic_backend:latest
    container_name: tele-celery-urban-nature
    command: >
      celery -A workers.task_queue worker
      --queues=q_urban_nature
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-urban-nature@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-urban-mental-health:
    image: csic_backend:latest
    container_name: tele-celery-urban-mental-health
    command: >
      celery -A workers.task_queue worker
      --queues=q_urban_mental_health
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-urban-mental-health@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-scenic-quality:
    image: csic_backend:latest
    container_name: tele-celery-scenic-quality
    command: >
      celery -A workers.task_queue worker
      --queues=q_scenic_quality
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-scenic-quality@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-hra:
    image: csic_backend:latest
    container_name: tele-celery-hra
    command: >
      celery -A workers.task_queue worker
      --queues=q_hra
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-hra@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-wave-energy:
    image: csic_backend:latest
    container_name: tele-celery-wave-energy
    command: >
      celery -A workers.task_queue worker
      --queues=q_wave_energy
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-wave-energy@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-scenario-gen:
    image: csic_backend:latest
    container_name: tele-celery-scenario-gen
    command: >
      celery -A workers.task_queue worker
      --queues=q_scenario_gen
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-scenario-gen@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  celery-worker-coastal-vuln:
    image: csic_backend:latest
    container_name: tele-celery-coastal-vuln
    command: >
      celery -A workers.task_queue worker
      --queues=q_coastal_vuln
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-coastal-vuln@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-wind-energy:
    image: csic_backend:latest
    container_name: tele-celery-wind-energy
    command: >
      celery -A workers.task_queue worker
      --queues=q_wind_energy
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --max-tasks-per-child=50
      --hostname=worker-wind-energy@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
      - ${HOST_MODEL_DATA_PATH:-/data/model_data}:/data/model_data:ro
      - ./datainput_for_demo:/data/datainput:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G

  celery-worker-ols:
    image: csic_backend:latest
    container_name: tele-celery-ols
    command: >
      celery -A workers.task_queue worker
      --queues=q_ols
      --concurrency=2
      --pool=prefork
      --loglevel=info
      --hostname=worker-ols@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G

  celery-worker-famd:
    image: csic_backend:latest
    container_name: tele-celery-famd
    command: >
      celery -A workers.task_queue worker
      --queues=q_famd
      --concurrency=1
      --pool=prefork
      --loglevel=info
      --hostname=worker-famd@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G

  celery-worker-co2:
    image: csic_backend:latest
    container_name: tele-celery-co2
    command: >
      celery -A workers.task_queue worker
      --queues=q_co2,q_cba,q_pop_density
      --concurrency=2
      --pool=prefork
      --loglevel=info
      --hostname=worker-co2@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  celery-worker-spatial-flows:
    image: csic_backend:latest
    container_name: tele-celery-spatial-flows
    command: >
      celery -A workers.task_queue worker
      --queues=q_radial_flows,q_commodity_trade,q_add_media
      --concurrency=2
      --pool=prefork
      --loglevel=info
      --hostname=worker-spatial-flows@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G

  celery-worker-telecoupling-pts:
    image: csic_backend:latest
    container_name: tele-celery-tc-pts
    command: >
      celery -A workers.task_queue worker
      --queues=q_add_agents,q_draw_agents,q_add_causes,q_add_systems,q_draw_systems
      --concurrency=2
      --pool=prefork
      --loglevel=info
      --hostname=worker-tc-pts@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  celery-worker-food:
    image: csic_backend:latest
    container_name: tele-celery-food
    command: >
      celery -A workers.task_queue worker
      --queues=q_food_security,q_nutrition
      --concurrency=2
      --pool=prefork
      --loglevel=info
      --hostname=worker-food@%h
    env_file: .env.docker
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/data/outputs
      - ${HOST_UPLOADS_DIR:-/data/uploads}:/data/uploads
    depends_on:
      redis:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  # ── Redis ────────────────────────────────────────────────────────────────
  redis:
    image: redis:alpine
    container_name: tele-redis
    command: redis-server --maxmemory 1gb --maxmemory-policy allkeys-lru
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: always

  # ── React frontend (nginx static) ───────────────────────────────────────
  frontend-ui:
    build: ./frontend
    image: csic_frontend:latest
    container_name: tele-frontend
    expose:
      - "80"
    depends_on:
      - api-server
    restart: always

  # ── File server (tool output downloads) ─────────────────────────────────
  # Exposed on 8001 for direct access — restrict in AWS security group
  # or route through nginx if you prefer single-port setup
  file-server:
    image: nginx:alpine
    container_name: tele-fileserver
    ports:
      - "8001:80"
    volumes:
      - ${HOST_SHARED_DIR:-/data/outputs}:/usr/share/nginx/html/download:ro
      - ./nginx/file-server.conf:/etc/nginx/nginx.conf:ro
    restart: always
