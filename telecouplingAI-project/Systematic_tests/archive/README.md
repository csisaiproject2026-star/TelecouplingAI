# Retired test artifacts

This directory preserves superseded and one-off test scripts for provenance.
It is excluded from default pytest discovery and is not an active validation
surface.

Do not update these scripts to support current deployments. If historical
behavior must be reproduced, first review each script for obsolete absolute
paths, URLs, dependencies, and test data.
